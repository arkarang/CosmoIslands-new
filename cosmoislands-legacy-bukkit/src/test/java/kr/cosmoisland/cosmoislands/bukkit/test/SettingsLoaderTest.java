package kr.cosmoisland.cosmoislands.bukkit.test;

public class SettingsLoaderTest {
    /*
    static SettingsLoader loader;
    static Database database;

    @BeforeClass
    public static void setUp() {
        database = TestKit.getDB();
        loader = new SettingsLoader(TestKit.getSETTINGS_TABLE(), TestKit.getDB());

            loader.load();
            loader.set();
            loader.get();
            loader.delete();
            loader.init();
            loader.asMap();

    }
    */
}
